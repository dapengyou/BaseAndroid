<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.test.baseandroid">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.broadCast.BroadActivity" />
        <activity android:name=".ui.broadCast.BroadCastActivity" />
        <activity android:name=".ui.broadCast.BroadCastTestActivity" />

        <!-- <receiver -->
        <!-- android:name=".BroadCastTestActivity$MyReceiver" -->
        <!-- android:enabled="true" -->
        <!-- android:exported="true"> -->
        <!-- <intent-filter> -->
        <!-- <action android:name="com.test.baseandroid.MY_BROADCAST" /> -->
        <!-- </intent-filter> -->
        <!-- </receiver> -->

        <activity android:name=".ui.sqlLite.SqlLiteActivity"></activity>
    </application>

</manifest>